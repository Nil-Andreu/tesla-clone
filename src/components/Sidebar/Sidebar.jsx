import React from 'react'
import {Container} from './SidebarComponents'

function Sidebar({isOpen, toggle}) {
    return (
        <Container isOpen={isOpen}>
            
        </Container>
    )
}

export default Sidebar
